kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: model-storage-lso
  namespace: experiment-01
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Ti
  storageClassName: local-sc-gpu
  volumeMode: Filesystem